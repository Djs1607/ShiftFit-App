Mid-workout numeric entry — big mono readout between two 40px targets, so it works with one thumb and gloves on.

```jsx
<Stepper label="Load" value={82.5} step={2.5} unit="kg" onChange={setLoad} />
```
