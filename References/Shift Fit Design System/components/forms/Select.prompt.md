Dropdown for 6+ mutually exclusive options (shift pattern, unit system). Fewer than four short options → use SegmentedControl instead.

```jsx
<Select label="Shift pattern" options={['4 on / 4 off','Rotating','Nights only']} />
```
