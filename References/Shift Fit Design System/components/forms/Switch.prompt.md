Settings toggle with immediate effect — label on the left, control on the right, full row width.

```jsx
<Switch checked label="Night-shift mode" description="Dims charts and shifts your day boundary to 12:00." onChange={setOn} />
```
