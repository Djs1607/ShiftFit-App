Linear determinate track. Amber for in-progress effort, blue for neutral progress, green when complete.

```jsx
<ProgressBar label="Session" value={3} max={5} valueLabel="3 / 5 sets" tone="accent" />
```
