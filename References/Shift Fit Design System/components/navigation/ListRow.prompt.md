The list unit — sessions, exercises, settings. 44px minimum height; mono `meta` on the right keeps numbers aligned down the column.

```jsx
<ListRow title="Bench press" subtitle="Chest · Barbell" meta="4 × 8" onClick={open} />
```
