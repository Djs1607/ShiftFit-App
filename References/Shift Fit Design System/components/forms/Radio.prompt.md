Single choice from a short list where each option needs a description (goal, shift pattern). Blue ring, not amber — this is a setting, not an effort log.

```jsx
<Radio checked label="Rotating shifts" description="Days and nights in the same week" />
```
