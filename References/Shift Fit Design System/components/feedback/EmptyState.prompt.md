Zero-data state. Never apologise, never joke — say what will appear here and give one way to start it.

```jsx
<EmptyState icon="moon" title="No sleep data yet"
  message="Log a sleep window after your next shift and the trend starts here."
  action={<Button variant="secondary" icon="plus">Log sleep</Button>} />
```
