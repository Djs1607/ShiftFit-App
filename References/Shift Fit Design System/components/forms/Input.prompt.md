Single-line field. Labels are uppercase micro-caps; units go in `suffix` and render in mono.

```jsx
<Input label="Load" type="number" defaultValue="82.5" suffix="kg" />
<Input label="Email" icon="mail" error="Not a valid address" />
```
