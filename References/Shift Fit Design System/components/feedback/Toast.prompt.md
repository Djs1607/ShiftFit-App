Transient confirmation, bottom-anchored above the tab bar, 4s auto-dismiss, rises 8px on entry.

```jsx
<Toast tone="success" title="Set logged" message="82.5 kg × 8. Two sets to go." />
```

Never stack more than two. Anything the user must decide belongs in a Dialog.
