Shift Fit's signature object: a 270° arc showing the fatigue score, coloured by band (fresh → critical).

```jsx
<FatigueGauge value={62} caption="Third night in a row. Keep it to technique work." />
```

One per screen, never two side by side. The arc fills once on entry over 900ms with mechanical easing — an instrument needle, not a bounce.
