Short definition on hover/focus — best used on metric labels ("Readiness", "Sleep debt") and icon-only buttons.

```jsx
<Tooltip label="Rolling 7-day load ÷ 28-day load"><Icon name="info" /></Tooltip>
```

One line only. If it needs a sentence and a half, put it in the Card as a caption.
