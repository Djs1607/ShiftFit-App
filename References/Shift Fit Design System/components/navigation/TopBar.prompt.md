Every screen's header. Title in Space Grotesk 19px; use `eyebrow` for shift context rather than a subtitle line.

```jsx
<TopBar eyebrow="Night 3 of 4" title="Today" actions={<IconButton icon="bell" label="Alerts" />} />
```
